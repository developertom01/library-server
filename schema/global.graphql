interface Error {
  message: String!
}

type UnAuthorizedError implements Error {
  message: String!
}

type PaginatedMeta {
  "Next page"
  nextPage: Int
  "Size of page"
  pageSize: Int!
  "Total Count of items"
  count: Int!
}

scalar Upload
scalar Url
scalar UUID
